rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users full read/write access to all documents
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

